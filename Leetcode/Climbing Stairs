class Solution {
public:
    int climbStairs(int n) {
        
        int previous = 0; 
        int current = 1; 
        int temp;

        for(int i = 1 ; i <= n ; i++){
            temp = current; 
            current+=previous;
            previous = temp; 
        }
        return current;
    }
};
